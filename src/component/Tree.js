import { Typography } from '@mui/material';
import React from 'react';

const Tree = () => {
    return(<>
        <Typography sx={{fontWeight: 700, fontSize: '20px'}}>Prerequisite Tree</Typography>
        <hr className='BottomBorderLine' />
        <br />
    </>)
}

export default Tree;